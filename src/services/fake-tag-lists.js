const tags = {
  tags: ['odd', 'even', 'only the first Article', '3/9/16/27'],
};

/**
 *  Gets tags.
 *
 * @return {string[]} tags.
 */
const getTags = () => {
  return tags;
};

export default getTags;
